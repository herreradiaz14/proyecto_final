import { DirectivaPrincipalDirective } from './directiva-principal.directive';

describe('DirectivaPrincipalDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaPrincipalDirective();
    expect(directive).toBeTruthy();
  });
});
